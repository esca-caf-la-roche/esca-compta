# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environnement (Convex & Vite)
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Reports
audit_convex.md

# Agents & Skills
.agents/
skills-lock.json

# Config Claude Code : on ignore tout .claude/ par défaut...
.claude/*
# ...mais on versionne la config partagée (hooks + réglages du projet).
!.claude/settings.json
!.claude/hooks/
# L'équipe d'agents du projet (Agent tool, subagent_type).
!.claude/agents/
# settings.local.json reste ignoré (permissions/chemins propres à la machine).

# Skills : seulement ceux écrits pour le projet (pas les skills Convex
# auto-installés via `npx convex ai-files install`, régénérables).
!.claude/skills/
.claude/skills/*
!.claude/skills/gestion-utilisateurs/
!.claude/skills/tuile-saison/
!.claude/skills/equipe/

# Projet de référence (suivi paiement - source Supabase)
suivi-paiement-cours/

# Sauvegardes Convex (exports de données)
backups/

# Fichiers sources de données (budget / salaires)
Salaire be.csv
planning_cours.csv
Budget_Escalade_2024-25.xlsx
*.xlsx
.~lock.*#

# Bulletins de salaire / documents avec données personnelles (RGPD)
BS.pdf
*.pdf

# Clés JWKS générées (Convex Auth) — public mais propre au déploiement local
jwks.json
